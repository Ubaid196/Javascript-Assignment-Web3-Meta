/**in this program I formed array of names and then run a for 
loop which access each person's name once then print it*/
var names = ["Ubaid", "Saad", "Asad", "Zaryab", "Abbas"];
for (var i = 0; i < names.length; i++) {
  console.log(names[i]);
}
